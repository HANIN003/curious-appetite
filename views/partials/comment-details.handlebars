{{!-- Temporary File so selectrecipe.handlebars would run properly --}}
<section class="card">
    <header>{{user.name}}</header>
    <div class="card-body">
      <p>{{content}}</p>
    </div>
  </a>
</section>
